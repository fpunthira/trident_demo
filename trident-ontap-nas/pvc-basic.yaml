kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: basic
  annotations:
    volume.beta.kubernetes.io/storage-class: basic-csi
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi